# apiVersion: v1
# kind: Secret
# metadata:
#   name: actual-budget
#   labels:
#     app: actual-budget
# type: Opaque
# stringData:
#   ACTUAL_OPENID_DISCOVERY_URL: "https://accounts.google.com/.well-known/openid-configuration"
#   ACTUAL_OPENID_CLIENT_ID: "<openid-client-id>"
#   ACTUAL_OPENID_CLIENT_SECRET: "<openid-client-secret>"
#   # must match the redirect URI registered in Google Cloud, minus /openid/callback
#   ACTUAL_OPENID_SERVER_HOSTNAME: "https://ab.s2n.donkeysharp.xyz"
#   # sessions default to never expiring; follow Google's token lifetime instead
#   ACTUAL_TOKEN_EXPIRATION: "openid-provider"
