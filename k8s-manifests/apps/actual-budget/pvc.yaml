apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget
  labels:
    app: actual-budget
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
