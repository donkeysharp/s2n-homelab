Public keys
===

Add the `ansible.pub` file, this file is the SSH public key file that will be added to `ansible` user authorized keys. It would be better if it does not have a passphrase to make the ansible experience smoother 😌.
